/*overload + add_ints(left: int, right: int) -> int {
  return core::op::add(left, right);
  }*/

def do_something(val: int) -> int {
    return val;
}

def do_something() -> bool {
    return true;
}

public
def entry() {
    let my_int: bool = do_something();
    let my_other: int = do_something(19int);
}
