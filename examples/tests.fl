import tests2;

public
def entry() -> int {
    let x: int = 10;
    if (10 > 10) {
        x = 10;
    } else if (false) {
        --x = 100;
        core::fmt::print_int(10);
    } else {
        core::fmt::print_int(100);
    }

    return x;
}
