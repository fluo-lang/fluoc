unit units {
    type km = int;

    def get_distance() -> km {
        let my_distance: km = 10;
        return my_distance;
    }
}

def get_distance() -> units::km {
    let my_distance: units::km = 10;
    return my_distance;
}
