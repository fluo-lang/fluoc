import random;

func random_num() -> int {
    random.int(10)
}

func entry() {
    let random_num = random_num();

    println(match random_int {
        1 => "one";
        5 => "five";
        7 => "seven";
        _ => "other: {_}";
    })?;
}
