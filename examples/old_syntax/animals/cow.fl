global global_warming: int = 0;

class Cow (weight: int, color: str, gender: str) {
    func init() {
        match gender.lower() {
            "m" => _;
            "f" => _;
            _ => { 
                println("Invalid Gender!"); 
                quits();
            };
        }
    }

    func fart() {
        global_warming++;
        println("You have just messed up the earth one more bit!");
    }
}

