class Animal {
    let pub farts: int;
    let weight: int;

    def fart() {
        println("You have just made global warming worse!");
        this.farts += 1;
    }

    pattern std::oop::new (weight: int) {
        return Animal { farts = 0, weight: weight };
    }
}

