let factorial = (value: long) -> long {
    if value == 0 {
        return 1;
    } else {
        return value * factorial(value - 1);
    }
};

let entry = () {
    std::fmt::print(factorial(20));
};

