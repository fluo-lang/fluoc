# STDOUT == 12\n123
# DESCRIPTION == Print values of point

rec Point = Point {
    x: I32,
    y: I32
}

fun entry () {
    let point = Point { x: 12, y: 123 }
    print(point.x)?
    print(point.y)?
}
