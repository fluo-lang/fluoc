# STDOUT == 10\n
# DESCRIPTION == Maybe with pattern match, some 10

rec Maybe[T] = Some { value: T } | Nothing;

fun entry () {
    pattern Maybe.Some { value: 10 } {
        Maybe.Some { value } -> print(value)
        Maybe.Nothing -> print("nothing")
    }
}
