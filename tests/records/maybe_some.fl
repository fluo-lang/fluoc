# STDOUT == 10\n
# DESCRIPTION == Maybe with pattern match, some 10

rec Maybe[T] = Some T | Nothing;

fun entry () {
    pattern Maybe.Some 10 {
        Maybe.Some val -> print(val)?
        Maybe.Nothing -> print("nothing")?
    }
}
