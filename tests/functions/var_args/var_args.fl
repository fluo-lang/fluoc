# STDOUT == [123, 123, 123]\n
# DESCRIPTION == Variable args

import std.string.Show;

# [] (array) implements std.iter.Collect, so we can use it as a wrapper
fun vararg[T: Show](...test: [T]) :: I32 {
    print(test)
}

fun entry () {
    vararg(123, 123, 123)
}
